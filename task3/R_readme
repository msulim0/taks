Task3 in R with dplyr.

Хід роботи:
  1) дата клінінг в power query
    1.1) customerid is not null
    1.2) lowercase for titles\
    
  2) Загружаємо data.csv в RStudio
    
  3) invoice total by price
    3.1) селектимо необхідні нам дані із створеної дати
    3.2) групуємо по invoiceno, invoicedate
    3.3) створюємо нову колонку за допомогою mutate(), яка рахує суму чеку(unitprice * quantity)
    3.4) сумуємо через summarazi() нашу новостворену колонку
    3.5) Рейнджимо по даті чеку
    
  4) total amount & quantity by customer
    4.1) селектимо необхідні нам дані із створеної дати
    4.2) групуємо по customerid
    4.3) створюємо нову колонку за допомогою mutate(), яка рахує суму чеку(unitprice * quantity)
    4.4) сумуємо через summarazi() нашу новостворену колонку і рахуємо кіл-ть ордерів за допомогою n()
    
  5) customer order by date
    5.1) селектимо необхідні нам дані із створеної дати
    5.2) групуємо по customerid та invoicedate
    5.3) створюємо нову колонку за допомогою mutate(), яка рахує суму чеку(unitprice * quantity)
    5.4) сумуємо через summarazi() нашу новостворену колонку і рахуємо кіл-ть ордерів за допомогою n()
    5.5) рейнджимо по customerid
    
  6) Для того аби створити індекс ордеру за датою та кастомером і колонки що виводять минулу дату замовлення
нам необхідно було б написати скріпт, який б:
    6.1) групував кастомерів по інвойсах(one cm - many inv);
         виводив дату інвойсу теперішню, а попередню рахував, як значення минулого рядка з датою із звʼязком до номера клієнта
    6.2) вивести номер замовлень по клієнту, вдалось частково зробити у табличці by_amount. Там колонка total_or виводить кіл-ть замовлень зроблених
одним клієном. Але як вивести цю кількість по окремо взятих інвойс намберах - додумати часу не вистарчило :)
